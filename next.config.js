/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', '127.0.0.1', 'gerbas-tani.com','tailwindui.com', 'images.unsplash.com', 'purepng.com', 'drive.google.com']
    },
  }

module.exports = nextConfig